# Scenario: execute_command — invoke Close command

Execute ApplicationCommands.Close on a dialog window.

```execute_command
{"locator": "//Window[@Name='SettingsDialog']", "command": "ApplicationCommands.Close"}
```
