# Scenario: resolve_binding — resolve a data binding expression

Resolve the binding on a TextBox and return the source path.

```resolve_binding
{"locator": "//TextBox[@Name='NameBox']", "property": "Text"}
```
