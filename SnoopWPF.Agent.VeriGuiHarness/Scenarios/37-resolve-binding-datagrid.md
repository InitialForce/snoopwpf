# Scenario: resolve_binding — DataGrid column binding

Resolve the binding on the first DataGrid cell in column 0.

```resolve_binding
{"locator": "//DataGrid[@Name='ItemsGrid']//DataGridCell[1]", "property": "Content"}
```
