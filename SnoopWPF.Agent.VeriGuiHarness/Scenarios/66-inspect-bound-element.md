# Scenario: resolve_binding → inspect_element — binding then full inspect

Resolve binding then do a full inspection of the same element.

```resolve_binding
{"locator": "//TextBlock[@Name='NameDisplay']", "property": "Text"}
```

```inspect_element
{"locator": "//TextBlock[@Name='NameDisplay']"}
```
