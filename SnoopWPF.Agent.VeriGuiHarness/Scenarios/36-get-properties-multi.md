# Scenario: get_properties — retrieve multiple properties at once

Read Width, Height, Visibility, and IsEnabled from a panel.

```get_properties
{"locator": "//Grid[@Name='ContentPanel']", "properties": ["Width","Height","Visibility","IsEnabled"]}
```
