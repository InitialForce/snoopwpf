# Scenario: find_elements — locate a Button by name

Find a Button named "OkButton" in the visual tree.

```find_elements
{"locator": "//Button[@Name='OkButton']"}
```
expect: //Button[@Name='OkButton']
