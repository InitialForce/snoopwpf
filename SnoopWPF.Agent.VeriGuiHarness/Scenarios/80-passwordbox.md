# Scenario: set_text_value — type into PasswordBox

Set a password value in a PasswordBox control.

```set_text_value
{"locator": "//PasswordBox[@Name='PasswordField']", "value": "s3cr3t!"}
```

```get_properties
{"locator": "//PasswordBox[@Name='PasswordField']", "properties": ["Password"]}
```
