# Scenario: click → get_ancestors — click link and check ancestor chain

Click a hyperlink and walk up its ancestor hierarchy.

```click
{"locator": "//Hyperlink[@Name='MoreInfoLink']"}
```

```get_ancestors
{"locator": "//Hyperlink[@Name='MoreInfoLink']"}
```
