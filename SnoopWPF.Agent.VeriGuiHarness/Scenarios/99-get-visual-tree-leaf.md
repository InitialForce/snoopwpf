# Scenario: get_visual_tree — depth 0 returns single element

Get the visual tree with depth 0 to return only the root element.

```get_visual_tree
{"locator": "//Button[@Name='CloseBtn']", "depth": 0}
```
